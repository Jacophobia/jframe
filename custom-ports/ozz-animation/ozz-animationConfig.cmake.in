# ozz-animation CMake Config File
# Generated by vcpkg

if(TARGET ozz::animation)
    return()
endif()

# Compute the installation prefix relative to this file.
get_filename_component(_IMPORT_PREFIX "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)

# ozz_base - foundation library
add_library(ozz::base STATIC IMPORTED)
find_library(OZZ_BASE_LIBRARY_RELEASE NAMES ozz_base_r ozz_base PATHS "${_IMPORT_PREFIX}/lib/" NO_DEFAULT_PATH)
find_library(OZZ_BASE_LIBRARY_DEBUG NAMES ozz_base_d ozz_base PATHS "${_IMPORT_PREFIX}/debug/lib/" NO_DEFAULT_PATH)

if(OZZ_BASE_LIBRARY_DEBUG)
    set_target_properties(ozz::base PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${OZZ_BASE_LIBRARY_RELEASE}"
        IMPORTED_LOCATION_DEBUG "${OZZ_BASE_LIBRARY_DEBUG}"
    )
else()
    set_target_properties(ozz::base PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        IMPORTED_CONFIGURATIONS "RELEASE"
        IMPORTED_LOCATION_RELEASE "${OZZ_BASE_LIBRARY_RELEASE}"
    )
endif()

# ozz_animation - runtime animation library
add_library(ozz::animation STATIC IMPORTED)
find_library(OZZ_ANIMATION_LIBRARY_RELEASE NAMES ozz_animation_r ozz_animation PATHS "${_IMPORT_PREFIX}/lib/" NO_DEFAULT_PATH)
find_library(OZZ_ANIMATION_LIBRARY_DEBUG NAMES ozz_animation_d ozz_animation PATHS "${_IMPORT_PREFIX}/debug/lib/" NO_DEFAULT_PATH)

if(OZZ_ANIMATION_LIBRARY_DEBUG)
    set_target_properties(ozz::animation PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${OZZ_ANIMATION_LIBRARY_RELEASE}"
        IMPORTED_LOCATION_DEBUG "${OZZ_ANIMATION_LIBRARY_DEBUG}"
    )
else()
    set_target_properties(ozz::animation PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE"
        IMPORTED_LOCATION_RELEASE "${OZZ_ANIMATION_LIBRARY_RELEASE}"
    )
endif()

# ozz_animation_offline - offline animation processing library
add_library(ozz::animation_offline STATIC IMPORTED)
find_library(OZZ_ANIMATION_OFFLINE_LIBRARY_RELEASE NAMES ozz_animation_offline_r ozz_animation_offline PATHS "${_IMPORT_PREFIX}/lib/" NO_DEFAULT_PATH)
find_library(OZZ_ANIMATION_OFFLINE_LIBRARY_DEBUG NAMES ozz_animation_offline_d ozz_animation_offline PATHS "${_IMPORT_PREFIX}/debug/lib/" NO_DEFAULT_PATH)

if(OZZ_ANIMATION_OFFLINE_LIBRARY_DEBUG)
    set_target_properties(ozz::animation_offline PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::animation"
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${OZZ_ANIMATION_OFFLINE_LIBRARY_RELEASE}"
        IMPORTED_LOCATION_DEBUG "${OZZ_ANIMATION_OFFLINE_LIBRARY_DEBUG}"
    )
else()
    set_target_properties(ozz::animation_offline PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::animation"
        IMPORTED_CONFIGURATIONS "RELEASE"
        IMPORTED_LOCATION_RELEASE "${OZZ_ANIMATION_OFFLINE_LIBRARY_RELEASE}"
    )
endif()

# ozz_geometry - geometry utilities library
add_library(ozz::geometry STATIC IMPORTED)
find_library(OZZ_GEOMETRY_LIBRARY_RELEASE NAMES ozz_geometry_r ozz_geometry PATHS "${_IMPORT_PREFIX}/lib/" NO_DEFAULT_PATH)
find_library(OZZ_GEOMETRY_LIBRARY_DEBUG NAMES ozz_geometry_d ozz_geometry PATHS "${_IMPORT_PREFIX}/debug/lib/" NO_DEFAULT_PATH)

if(OZZ_GEOMETRY_LIBRARY_DEBUG)
    set_target_properties(ozz::geometry PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${OZZ_GEOMETRY_LIBRARY_RELEASE}"
        IMPORTED_LOCATION_DEBUG "${OZZ_GEOMETRY_LIBRARY_DEBUG}"
    )
else()
    set_target_properties(ozz::geometry PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE"
        IMPORTED_LOCATION_RELEASE "${OZZ_GEOMETRY_LIBRARY_RELEASE}"
    )
endif()

# ozz_options - command line options library
add_library(ozz::options STATIC IMPORTED)
find_library(OZZ_OPTIONS_LIBRARY_RELEASE NAMES ozz_options_r ozz_options PATHS "${_IMPORT_PREFIX}/lib/" NO_DEFAULT_PATH)
find_library(OZZ_OPTIONS_LIBRARY_DEBUG NAMES ozz_options_d ozz_options PATHS "${_IMPORT_PREFIX}/debug/lib/" NO_DEFAULT_PATH)

if(OZZ_OPTIONS_LIBRARY_DEBUG)
    set_target_properties(ozz::options PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${OZZ_OPTIONS_LIBRARY_RELEASE}"
        IMPORTED_LOCATION_DEBUG "${OZZ_OPTIONS_LIBRARY_DEBUG}"
    )
else()
    set_target_properties(ozz::options PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
        INTERFACE_LINK_LIBRARIES "ozz::base"
        IMPORTED_CONFIGURATIONS "RELEASE"
        IMPORTED_LOCATION_RELEASE "${OZZ_OPTIONS_LIBRARY_RELEASE}"
    )
endif()

# Provide legacy variable names
set(ozz-animation_FOUND TRUE)
set(OZZ_ANIMATION_FOUND TRUE)
set(OZZ_ANIMATION_INCLUDE_DIRS "${_IMPORT_PREFIX}/include")
set(OZZ_ANIMATION_LIBRARIES ozz::animation ozz::animation_offline ozz::geometry ozz::base)

unset(_IMPORT_PREFIX)
