-- config/camera.lua
-- Camera configuration

return {
    zoom = 1.0,
    followSmoothing = 0.0,  -- 0 = instant follow
    deadzone = { x = 0.0, y = 0.0 },
    shake = {
        magnitude = 10.0,
        duration = 0.3
    }
}
