-- inputs.lua - Input action definitions
-- This game uses the legacy polling API (bestow.input.wasKeyJustPressed)
-- No action registrations needed - input.lua handles polling directly

-- Set initial phase to satisfy startup requirement
-- The game manages its own phases via app.state.currentPhase
