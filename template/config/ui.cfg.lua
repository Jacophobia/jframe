-- UI configuration
-- All fields are explicit with sane defaults. Modify as needed.

return {
    baseScale       = 1.0,          -- UI element scale multiplier
    enableDebugMode = false,        -- Show element outlines and debug info
    themePath       = "assets/ui/theme.rcss",  -- Global RCSS theme
    fonts           = {
        "assets/fonts/default.ttf", -- Primary UI font (placeholder — replace with your font)
    },
}
