-- UI configuration

return {
    baseScale       = 1.0,
    enableDebugMode = false,
    themePath       = "assets/ui/theme.rcss",
    fonts           = {
        -- Add font paths here, e.g.:
        -- "assets/fonts/roboto.ttf",
    },
}
